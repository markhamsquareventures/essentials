---
allowed-tools: Bash(php artisan test:*), Bash(./vendor/bin/pint:*), Bash(npm run types:*)
description: Run tests, lint, and type checks
---

## Your task

Run the full test/lint/types suite in order:

1. Run tests: `php artisan test`
2. Lint PHP: `./vendor/bin/pint`
3. Check TypeScript: `npm run types`

Report any failures clearly. If everything passes, confirm success.
